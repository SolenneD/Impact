{% extends 'base.html.twig' %}

{% block body %}

    <table>
        {% for user in users %}
            <tr >
                <td>
                    {{ user.name }}
                </td>
                <td>
                    {{ user.lastname }}
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}